# Neon Database URL
# Database Configuration (Neon PostgreSQL)
# Use the pooled connection for production
DATABASE_URL=postgresql://your_user:your_password@your_host/your_database?sslmode=require

# Optional: For direct connections without pgbouncer
DATABASE_URL_UNPOOLED=postgresql://your_user:your_password@your_host/your_database?sslmode=require

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Future authentication (not required for Fastlight)
# NEXT_PUBLIC_STACK_PROJECT_ID=
# NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
# STACK_SECRET_SERVER_KEY=

# Blink API (optional - defaults to public endpoint)
BLINK_API_URL=https://api.blink.sv/graphql

# Optional: API Key for authenticated requests (not needed for username verification)
# BLINK_API_KEY=
